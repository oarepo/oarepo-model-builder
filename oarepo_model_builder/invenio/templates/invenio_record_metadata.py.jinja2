from invenio_db import db
from invenio_records.models import RecordMetadataBase

{% for b in python.record_metadata_bases %}
    import {{ b|package_name }}
{% endfor %}

class {{ python.record_metadata_class|base_name }}({% for b in python.record_metadata_bases %}{{ b }}, {% endfor %}db.Model, RecordMetadataBase):
    """Model for {{ python.record_class }} metadata."""

    __tablename__ = "{{ python.record_metadata_table_name }}"