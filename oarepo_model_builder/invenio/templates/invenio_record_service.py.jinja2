from invenio_records_resources.services import RecordService as InvenioRecordService


{% for b in model.record_service_bases %}
from {{ b|package_name }} import {{ b|base_name }}
{% endfor %}


class {{ model.record_service_class|base_name }}({% for b in model.record_service_bases %}{{ b|base_name }}{{ ", " if not loop.last else "" }}{% endfor %}):
    """{{ model.record_class|base_name }} service."""

{{ model.invenio_record_service_extra_code }}