from invenio_records.dumpers import SearchDumper


{% for b in model.record_dumper_bases %}
from {{ b|package_name }} import {{ b|base_name }}
{% endfor %}

class {{ model.record_dumper_class|base_name }}({% for b in model.record_dumper_bases %}{{ b|base_name }}, {% endfor %}SearchDumper):
    """{{ model.record_class|base_name }} opensearch dumper."""

{{ model.invenio_record_dumper_extra_code }}