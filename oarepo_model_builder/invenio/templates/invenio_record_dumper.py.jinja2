from invenio_records.dumpers import SearchDumper


{% for b in vars.record_dumper_bases %}
from {{ b|package_name }} import {{ b|base_name }}
{% endfor %}

class {{ vars.record_dumper_class|base_name }}({% for b in vars.record_dumper_bases %}{{ b|base_name }}, {% endfor %}SearchDumper):
    """{{ vars.record_class|base_name }} opensearch dumper."""

{{ vars.invenio_record_dumper_extra_code }}